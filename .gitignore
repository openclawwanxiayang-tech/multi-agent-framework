# OpenClaw personal/runtime artifacts (out of product repo scope)
.logs/
.openclaw/
.pi/

# Personal assistant identity/memory workspace files
# (tracked for cloud backup by user preference)

# Local skill/memory bundles (personal workspace)
skills/
memory/

# Sensitive/local secrets (keep out of Git)
.env
.env.*
*.pem
*.key
*.p12
*.pfx
secrets/
credentials/
